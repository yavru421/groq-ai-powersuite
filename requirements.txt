gradio>=4.0.0
# Only the core dependencies for gradio_ft.py and its features

groq>=0.5.0
python-dotenv==1.0.0
Pillow>=9.0
PyPDF2>=3.0.0
python-docx>=0.8.11
python-pptx>=0.6.21
pandas>=1.0
json5==0.9.5
cryptography
# Optional: For audio transcription (if used in production)
speechrecognition
soundfile

# The following are not needed for gradio_ft.py and can be removed:
# streamlit
# streamlit-option-menu
# streamlit-chat
# requests (unless you use it elsewhere)
# beautifulsoup4
# google-api-python-client
# face_recognition
# dlib
